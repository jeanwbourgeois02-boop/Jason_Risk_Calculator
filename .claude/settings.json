{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "for p in .venv/bin/python .venv/Scripts/python.exe python3 python; do if command -v \"$p\" >/dev/null 2>&1; then exec \"$p\" tools/lint_hook.py; fi; done; exit 0",
            "timeout": 30,
            "statusMessage": "ruff on the edited file"
          }
        ]
      }
    ]
  }
}
