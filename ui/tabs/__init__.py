"""Per-tab Dash modules for the risk monitor UI (the shell is ui-shell's; each tab module is its own agent's, see CLAUDE.md "Feature pairs").

Each tab in CLAUDE.md "Data contract -> Six tabs as views" gets its own module here as
it moves off the placeholder in ui/app.py. See ui/tabs/cash_ladder.py for the first one.
"""
