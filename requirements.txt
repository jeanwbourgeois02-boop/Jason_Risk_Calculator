dash>=4.4,<5
pandas>=3,<4
openpyxl>=3.1,<4
xlrd>=2,<3
tzdata
pytest>=8
# blpapi is NOT listed here: `py risk.py setup` installs it from Bloomberg's own index
# (https://blpapi.bloomberg.com/repository/releases/python/simple/) on the Bloomberg PC.
pyyaml>=6.0
