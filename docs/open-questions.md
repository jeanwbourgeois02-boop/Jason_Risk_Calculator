# Open questions

**2026-09-14 user decision:** HA-portfolio vJean is authoritative. Prior proposed defaults that alter its arithmetic are superseded. See `excel-parity-audit.md` and the correction at the top of CLAUDE.md. Full live FX numerical parity awaits valid rates; the supplied workbook caches have Bloomberg/Excel errors.

Unresolved items from the data-contract review of `data/raw/HA_PNL_20260818.csv` and `data/raw/HA-portfolio vJean.xlsx` (2026-09-13). Each has a proposed default; the default is what the app assumes until the question is closed. Settled decisions live in `CLAUDE.md`.

## Assumptions in force, unconfirmed

- **NDF list.** BRL, TWD, KRW, IDR treated as non-deliverable (`instruments.is_ndf = 1`); TRY and MXN treated as deliverable. To be verified with the PB. Affects the cash ladder only (NDF legs carry `settles_cash = 0`). `data/ingest/bnp.py` hard-codes this list (`NDF_CCYS`) as provisional.
- **`as_of_date` from the BNP filename.** `HA_PNL_YYYYMMDD.csv` dated T → `as_of_date` = previous weekday (Mon–Fri); no holiday calendar yet. On a day after a US holiday the caller must pass `as_of_date` explicitly. To be replaced by the trading calendar when it exists.
- **PB FUTURES rows create no `trades`.** The BNP FUTURES row is one netted position with no fill dates; the ingest writes a `positions` row (and the instrument) only. **Updated 2026-09-16:** futures fills (and FX forward/spot, option and IRS fills generally) now come from the blotter (`data/ingest/blotter.py`, `data/raw/new_sample_trades.csv`), which carries a genuine per-trade fill price and ID, wired into the app's one upload control (item 57, resolved). The xlsx workbook's `All FX trades` sheet (`C = contracts × 50 × fill`) is no longer read for futures fills at all: `data/ingest/xlsx_futures.py` was removed 2026-09-16 as dead code once its only caller (`ui/tabs/reconciliation.py`) was deleted (item 56). See item 55.
- **P&L identity tolerance in ingest.** The contract states `DTD Total = MV Base − Start Date Dirty MV`, `MTD Total = MV Base − Prev ME MV Base`, `DTD Total = DTD Trading` as identities; `bnp.py` checks them with its own float tolerance `PNL_TOL = 0.01 USD` (observed max deviation 6e-5 on the 08/18 file). Not a contract tolerance.

## Questions

1. **Quantity display / residuals.** Storage keeps exact legs; display uses USD notional per pair. Should residual base-currency exposure be surfaced (e.g. −24,310 AUD left by two offsetting 2,000,000-USD AUDUSD trades at different fills)? Default: show it in the delta tab, hide it in the FX tab.
2. **Portfolio hand edits.** `B15 (USDIDR) += N42` (USDJPY option delta −70.5m) and `B23 (USDTWD) += N37` (USDZAR delta +9.95m) look misaligned after the sorted spill gained USDIDR. `B9 (EURSEK) += B10` adds the EURUSD position to EURSEK — intentional net-EUR view? Default: deltas belong to USDJPY and USDZAR; the EURSEK + EURUSD add is dropped.
3. **Net / Gross USD scope.** In the sheet XAUUSD sits outside the summed range and ESU6 enters Net USD with −1 sign while ESZ6 is netted out via `+B8`. Should gold and equity futures be in Net/Gross USD? Default: FX pairs only; gold and futures shown as separate lines.
4. **RESOLVED 2026-09-17 — option deltas come from `engine/options` (`QL_OPTIONS_PRICER`, official for DELTA/PREMIUM/Greeks; MANUAL is reconciliation-only); the ladder's option branch reads them via `marks_official` (item 25).** Original question: Today hand-typed (USDJPY −8.5m, −17m, −45m; EURSEK 12,373,791 EUR; USDZAR 9,951,608). Source for the app: manual table, Bloomberg OVML, or PB? Units assumed = base-currency delta-equivalent notional (digitals can exceed the notional). Default: `marks.DELTA` with `source = MANUAL` until a feed exists.
5. **FX swaps — representation at BNP.** The 08/18 file contains no opposite-sign pair with different value dates, so it cannot show how BNP books a swap. A swap's near leg settles T+2 and drops out of the PB snapshot, so in any file older than two days a swap is indistinguishable from an outright; swaps can only be identified from the blotter or by diffing the daily archive. Does BNP deliver a separate trade/blotter file?
6. **FX swap candidates in the xlsx** (same pair, same trade date, opposite equal quantities). Different-tenor pairs matched by the rule and grouped as swaps: rows 303/304 USDCAD (2026-08-24, 08-25 → 09-16), 316/317 USDCAD (08-27, 08-28 → 09-16), 321/322 USDJPY (08-28, 08-31 → 09-16), 326/327 EURUSD (08-31, 09-02 → 09-16), 330/331 EURUSD (09-01, 09-03 → 09-16), 336/337 USDJPY (09-01, 09-16 → 2027-03-01), 341/342 USDCHF (09-01, 09-16 → 2027-09-03), 354/355 USDZAR (09-09, 09-11 → 10-13); 318/319 vs 320 USDJPY (2 × 7.5m near vs −15m far) needs manual grouping. **Same-tenor** opposite pairs, not grouped, to confirm as round trips or mis-keyed swap legs: 302/303 USDCAD ±25m both 08-25; 335/336 USDJPY ±23m both 2027-03-01 (fills 157.53 / 157.7071); 315/316 USDCAD ±25m both 08-28; 297/298 EURSEK 1.5m / −1,499,499 both 08-25; 305/306 and 307/308 EURSEK; 324/326 EURUSD ±13.5m both 09-02; 333/338 USDJPY ±16m both 09-03; plus 07-24 USDSGD, 07-30 EURUSD, 08-04 USDTRY, 08-06 USDKRW, 08-07 USDJPY, 08-14 USDBRL, 08-18 EURUSD, 08-20 USDCHF / USDZAR (all same tenor 09-16, present in the BNP file as separate round trips).
7. **RESOLVED 2026-09-17 — IRS direction user-confirmed: positive notional/position = pay fixed** (blotter rule below; the same convention applies to the BNP `Position` sign in `data/ingest/irs.py`). Earlier history: **PARTIALLY RESOLVED 2026-09-15 — IRS direction still `# UNVERIFIED`.** `data/ingest/irs.py` now parses IRS rows and infers direction from BNP `Position` sign (`Position > 0` → pay fixed), matching `swapcalc`'s (the ported reference project's) own convention — but that convention itself carries the same `# UNVERIFIED` flag in the reference project pending PM confirmation; it has not been independently confirmed against BNP. `Quantity` = notional in millions × 1e6, `Price` = fixed rate — confirmed by the three real IRS rows in the 08/18 file. Still open: PM confirmation of pay/receive sign, and whether the 1,216,900,000 swap dated 2026-08-18 appears on 08/19 (T−1 close consistency check not yet run). **Blotter rule reconfirmed 2026-09-17:** for the blotter (`data/ingest/blotter.py::_parse_irs`) the user set the rule on 2026-09-16 — positive `Notional` = pay fixed, negative = receive fixed, `Side` carries no direction — and reconfirmed it on 2026-09-17 (via session risk-monitor-31) after a `Side`-based cross-check (Buy = pay, Sell = receive, contradiction rejects) was proposed and declined. No cross-check; `Side` is ignored for IRS rows.
8. **RESOLVED 2026-09-15 — IRS pricing.** The Bloomberg-curve pricer is now built: `engine/rates/` (new agent **rates-pricer**) bootstraps single-currency OIS discount curves from a new `curve_quotes` staging table (populated by `data/bloomberg/rates_marketdata.py`, ported from the reference `swapcalc` project) and prices swaps via QuantLib, writing `curves` rows (`source='QL_PRICER'`) and `marks` rows (`PAR_RATE`/`PV_USD`/`DV01_USD`, `source='QL_PRICER'`, now official — see CLAUDE.md's official-marks table). Bloomberg SWPM (`BBG_BDH`) is reconciliation-only for these three mark_types, mirroring `BNP_BVAL` for FX. **Phase 1 scope: OIS only** (USD SOFR, EUR ESTR, GBP SONIA, JPY TONA, CHF SARON, CAD CORRA, AUD AONIA) — term-rate (EURIBOR/BBSW), basis and XCCY swaps are deferred to Phase 2, ported the same way once Phase 1 is reviewed and trusted.
9. **RESOLVED — matured trades.** `engine/pnl/ledger.realise_settled` freezes FX, futures, IRS and (since 2026-09-17) FX-option P&L at settlement/maturity/expiry into `realised_pnl`; `value_book` reads the frozen row and never re-marks a settled trade. Original: the xlsx kept marking them at the T+5 outright; BNP dropped settled forwards.
10. **Sources of truth per account.** The BNP file already carries GSCO-DRV, GSIL-FUT, JPML-IPB, NTXS-ISD, UBSI-ISD and HOLD-BOX rows. Is it the consolidated fund-level file for all positions, or must other PB files be ingested? Default: BNP file is consolidated.
11. **Strategy filter.** `HAHY7` (241 rows) vs `HACA` "Cash Adj" (1 row). Default: include both, tag by strategy.
12. **Mark time.** Decided 2026-09-15: official EOD is 17:00 New York (Bloomberg daily FX close). The xlsx 15:00 snapshot is reconciliation only.
13. **Gold.** XAUUSD treated as an FX pair (USD notional in xlsx; ounces at BNP). Keep as FX, or as a commodity line? Default: FX pair, excluded from Net/Gross USD (see 3).
14. **Crosses.** USD notional of EURSEK = EUR amount × EURUSD spot (sheet uses the live rate). Default: spot of `as_of_date`.
15. **Book parameters.** `pnl time series` holds capital 35m, vol allocation 4.5m, stop 5m, first stop 3.5m, daily vol = annual / 16. Config for the overall-book tab? Default: yes, in a config file, not in the data tables.
16. **Same-day trades in the PB file.** 22 forwards on 08/17 are in the 08/18 file, none from 08/18 itself. Is the PB cut-off end of day New York or London? Affects which trades the reconciliation expects.
17. **`positions` grain vs PK.** CLAUDE.md says "one row per PB position per day (BNP grain)", but BNP's FORWARD grain is one row per trade and the PK `(as_of_date, source, account, instrument_id, settle_date)` cannot hold that: the 08/18 file has e.g. three USDTRY 09/16/26 rows in BNPP-IPB-NMMF. The ingest nets rows sharing the key (sums quantity, cost, MV, P&L; asserts `Price` and `Fx` are identical within the group — true for all 31 groups in the reference file), giving 31 position rows for 239 PB rows. Per-trade granularity survives in `trades`. Options: keep netting (default), or add `trade_id` to the PK and store PB grain.
18. **Blank `Fx` / `Trade Factor` on zero-balance CURRENCY rows.** EUR, XAU, HKD and one UBSI USD cash row have zero balance and blank `Fx` and `Trade Factor`. `positions.fx_to_usd` is NOT NULL, so the ingest writes `0.0` (1.0 for USD) as a sentinel. Not in the contract. Default: keep 0.0 and have the engine take spot from `marks_official`; alternatively document the sentinel in CLAUDE.md.
19. **ESU6 `Position = 0` while `Quantity = 27`.** Forwards show `Position = Quantity`; the FUTURES row does not. Ingest uses `Quantity` as contracts (consistent with `MV = Quantity × Trade Factor × Price − Cost`, verified). Confirm with BNP what `Position` means for futures.
20. **Gold cash line.** `XAU.C-…` (zero balance today) is ingested as `CASH-XAU`, `asset_class = CASH`, `bbg_ticker = 'XAU Curncy'`. Should a non-zero gold balance be a cash row or an XAUUSD position? Default: cash row (related to 13).
21. **`bbg_ticker` for cash instruments** is not specified in the contract; ingest uses `'<CCY> Curncy'`. Confirm or set a sentinel.
22. **BUY-first FORWARD descriptions.** Only `SELL … VS .BUY …` occurs in the 08/18 file; the `BUY … VS .SELL …` branch of the regex is covered by synthetic tests only. Confirm with a file containing one.
24. **Decided follow-up (cash-ladder, after bbg-data delivers marks): NDF net settlement row.** Decision: excluding both NDF legs from the gross ladder is correct; an NDF delivers nothing gross. The net USD settlement (USD leg plus local leg converted at the forward mark to the fixing date) belongs in the ladder as a third row kind, `NDF_NET`, once `FWD_OUTRIGHT` marks exist in `marks_official`. Not an open question; queued for cash-ladder.
25. **Decided follow-up (cash-ladder, when options are built): `ladder.py` delta query to LEFT JOIN + NULL check.** Decision: the inner join on `SPOT` in the option quote-ccy branch was a contract bug; CLAUDE.md now specifies a `LEFT JOIN` and requires the engine to raise on any `NULL` delta rather than drop the leg silently. `engine/ladder/ladder.py` still embeds the inner-join form and must be updated to the contract query, with the NULL check, when options are built. **RESOLVED 2026-09-17** (options merge Phase 3): `ladder.py` now LEFT JOINs SPOT and raises pre-aggregation on any option lacking one. Also found and fixed while doing it: the contract SQL joined SPOT on the option's own `instrument_id`, which can never match a pair SPOT row — join is now on `i.base_ccy || i.quote_ccy` in both `ladder.py` and CLAUDE.md.
26. **Decided (bbg-data, 2026-09-13): `BBG_INTERP` marks source.** `data/bloomberg/pull_marks.py` writes `source = BBG_INTERP` for forward outrights it builds by linear interpolation in forward points between standard tenors when the direct broken-date request returns nothing. Never official (`marks_official` unchanged); reconciliation / pipeline fallback only. Recorded in CLAUDE.md. Not an open question.
27. **Bloomberg request for a broken-date forward outright — UNVERIFIED.** *(Probe steps `fwd_outright_direct_primary` = `FWD_CURVE` + `FWD_CURVE_QUOTE_FORMAT=OUTRIGHTS` + `SETTLE_DT`; `fwd_outright_direct_alt_reference_date` = same + `REFERENCE_DATE`; `fwd_outright_direct_alt_fwd_outright_field` = `FWD_OUTRIGHT_PRICE` + `SETTLE_DT`. Run `docs/bloomberg-run.md`, then `diagnose.py` on the probe JSON. If the recorded value is non-scalar, `FWD_CURVE` is bulk and the primary combination is wrong.)* `pull_marks.py` sends `ReferenceDataRequest`, field `FWD_CURVE`, overrides `FWD_CURVE_QUOTE_FORMAT = 'OUTRIGHTS'` and `SETTLE_DT = <YYYYMMDD>`, expecting a scalar outright. `FWD_CURVE` may in fact be a bulk (array) field; any non-numeric response falls through to the tenor-interpolation fallback (item 28), so a wrong guess degrades to `BBG_INTERP` marks rather than failing. To verify on the Bloomberg machine: run the script on a real request CSV and check whether any `FWD_OUTRIGHT` row comes back with `source = BBG_BFXFORWARD` (direct path worked) or all are `BBG_INTERP` (direct path failed). Alternative candidates if it fails: `FX_FWD_OUTRIGHT`-style fields with `SETTLE_DT`, or the `BFXFORWARD`-specific fields the xlsx uses (see the xlsx `BFXFORWARD` formulas for exact field names).
28. **Tenor-point fallback field names — UNVERIFIED.** *(Probe steps `tenor_1m`, `tenor_3m`, `fwd_points_scale`. A `PX_LAST` in the same order of magnitude as spot means it is an outright, not points.)* Fallback tickers `<PAIR><TENOR> Curncy` for `SP, 1W, 2W, 1M, 2M, 3M, 6M, 1Y` with `PX_LAST` = forward points and `SETTLE_DT` = that tenor's settlement date; points divisor from field `FWD_POINTS_SCALE` on `<PAIR> Curncy` (pulled live, not hard-coded, since it varies by pair). `ON` and `TN` are excluded (pre-spot convention: outright = spot − points, not +); settle dates before the SP date are rejected, never extrapolated. Confirm the three field names and that `PX_LAST` on a tenor ticker is points, not an outright.
29. **Spot mark: field and time.** *(Probe steps `spot_reference`, `spot_historical` show whether both paths return a value; they do not settle the 15:00-vs-close policy.)* `pull_marks.py` pulls `SPOT` as `HistoricalDataRequest PX_LAST` for `as_of_date` (a daily close, i.e. Bloomberg's NY 17:00 close, not 15:00 NY), while `snapped_at` is stamped 15:00 America/New_York per the mark-time convention. Options: accept the close as the official spot; use `BFIX` 15:00 NY fixings (`<PAIR> BFIX Curncy`); or run the script live at 15:00 NY with `ReferenceDataRequest PX_MID`. Default: daily close via `PX_LAST` until decided.
30. **Forward points are live only.** *(Probe evidence: `spot_historical` vs `tenor_*` — the tenor steps are `ReferenceDataRequest` only; no historical tenor step is probed yet.)* Tenor points and the direct outright come from `ReferenceDataRequest` (live); there is no historical broken-date outright. The script therefore refuses `--as-of` ≠ today (America/New_York) when any `FWD_OUTRIGHT` is requested unless `--allow-stale-as-of` is passed (then it warns). Operationally the pull must run on the Bloomberg machine on `as_of_date` itself, ideally at 15:00 NY. Is a `HistoricalDataRequest` on `<PAIR>1M Curncy` etc. acceptable for back-filling past dates (interpolated only, `BBG_INTERP`)?
31. **Futures settlement field.** *(Probe steps `es_settle_px_settle`, `es_settle_px_last`.)* `FUTURE_PX` is pulled as `HistoricalDataRequest PX_SETTLE` for `as_of_date` (matches BNP "Price = settlement price"). Confirm `PX_SETTLE` vs `PX_LAST` for ES. Related: `export_request` treats a future as closed on its expiry date (`expiry_date > as_of`, consistent with the delta query's `settle_date > :as_of`), so no `FUTURE_PX` is requested on expiry day; the P&L engine must freeze expiry-day LTD the same way (see item 9).
32. **BNP spot coverage is partial.** BNP `Fx` is quote-ccy→USD, so a pair spot is only derivable for USDXXX pairs (`1/Fx`). On XXXUSD rows `Fx = 1.0` and the spot is not derivable at all (`Price` is the forward outright); crosses need a row whose quote currency is the cross's base, which never occurs (there is no `USDEUR` row). On the 08/18 file `bnp_marks.py` emits SPOT for 14 of 18 pairs (none for AUDUSD, EURUSD, GBPUSD, XAUUSD) with a warning, and FWD_OUTRIGHT for all 19 `(pair, value_date)` keys. Does BNP expose a spot rate elsewhere (another column or file)? Default: partial `BNP_BVAL` SPOT coverage; reconciliation of XXXUSD pairs uses `FWD_OUTRIGHT` only.
33. **Bloomberg-machine Python prerequisites.** `pull_marks.py` needs Python ≥ 3.9 (`zoneinfo`, `dataclasses`) and `blpapi`. Since 2026-09-14 the zone is resolved lazily via `_ny()` rather than at import, but on Windows it still needs the `tzdata` pip package; since 2026-09-14 a missing package produces a `tz_prerequisite` diag entry, a stderr hint and exit 6 instead of a crash with no diagnostics (item 39, resolved). Nobody has confirmed the Python version, `blpapi` version or `tzdata` presence on the terminal machine. `docs/bloomberg-run.md` lists a preflight; the first probe run answers this.
34. **Probe broken date may be a non-business day.** The probe asks for the EURUSD outright at `--as-of + 30 days` without a calendar. If that date is a weekend or holiday, a failure on the direct request is ambiguous (bad field vs bad date). Default: if the probe fails on every candidate, re-run with an `--as-of` whose +30d is a weekday before concluding the field names are wrong. Better: let the probe pick the next weekday (bbg-data, when the first probe results are in).
35. **Batched tenor request classification.** `pull_marks.py` classifies the eight-ticker tenor request as a whole (`FIELD_EXCEPTION` if any one tenor ticker fails) while the CSV row can still be interpolated from the remaining tenors. Exit codes follow requested-vs-written keys, so this only affects the counts in the diagnostics report. Keep batch-level (default) or move to per-ticker classification once real responses show how often single tenors fail.
36. **LTD P&L product scope.** `engine/pnl/pnl.py::ltd_per_trade` covers `FX_FWD` only, taking `settle_date` from `leg_no = 1`. `FX_SPOT` trades with value date > `as_of` carry unrealised P&L and are dropped silently; `FX_SWAP` packages (4 legs, two value dates) would get the near leg's date only. Default: extend to `FX_SPOT` and `FX_SWAP` when the xlsx ingest lands, marking each leg at its own `settle_date` rather than via `leg_no`.
37. **Cross-pair spot lookup direction.** Quote→USD for a cross is derived only from a `USD<quote>` SPOT row (`1 / USDSEK` for EURSEK). A cross whose quote ccy is market-quoted XXXUSD (EURGBP → GBPUSD) returns NaN even when `GBPUSD` SPOT exists. Spec-compliant (NaN, never estimated) and no such cross is in the book today. Default: add a `<quote>USD` fallback when one appears.
38. **RESOLVED 2026-09-14.** `pull_marks.py:277` referenced the removed module constant `NY`; now calls the lazy loader `_ny()` like the other three call sites. Suite is 115 passed. Follow-up gap is item 39.
39. **RESOLVED 2026-09-14.** `main()` now resolves `_ny()` in `try/except` immediately after `Diagnostics()` and before the first `record_environment`; `ZoneInfoNotFoundError` becomes a `tz_prerequisite` failure with hint `py -3 -m pip install tzdata`, a written `.diag.json`, a stderr hint and exit 6 (added to the docstring table). Test `test_pull_marks_missing_tzdata_writes_diag_and_exits_6`; suite 116 passed. Reviewer reproduced with real tzdata blocked. Residuals in item 40. Original text: **Missing-tzdata path in `pull_marks.py` main() does not match its own docstring (bbg-data, reviewer warning 2026-09-14).** `_ny()`'s docstring says `main()` resolves the zone after `Diagnostics()` is created and turns `ZoneInfoNotFoundError` into a diag entry + stderr hint + exit 6. It does not: there is no `except ZoneInfoNotFoundError`, no exit 6 in the exit-code table, and the first `diag.record_environment(...)` (line ~1324) runs before the `try/except ... finally: write_diagnostics` block, so on a Windows terminal machine without `tzdata` the script crashes with no `.diag.json` written (reviewer reproduced by hiding `tzdata` and clearing the zoneinfo TZPATH). No test covers the path. Proposed: resolve `_ny()` in `try/except` right after `diag = Diagnostics()`, record stage `tz_prerequisite` with a `pip install tzdata` hint, write diagnostics, return 6; add 6 to the exit-code table; add a test that blocks `tzdata` and asserts exit 6 plus diag file present. Same root cause as item 33.

40. **Exit-6 diag has an empty `environment` block (reviewer warning 2026-09-14, open).** On the `tz_prerequisite` path `record_environment` cannot run because it itself calls `_ny()`, so the diag omits `python_version`, `hostname`, `machine_timezone`, `machine_time_local` -- the fields most useful for that failure. Proposed: record a partial environment (everything except `machine_time_america_new_york`) in the `except` block, or give `record_environment` an `ny_available` flag. Minor related notes: the test monkeypatches `pull_marks.ZoneInfo` rather than blocking `tzdata`/TZPATH and does not assert stderr or pull mode; `_ny()` caches in `_NY_ZONE`, so any future test of this branch must reset it or it passes vacuously; `diagnose.render_report` prints `=== failures (0) ===` for this diag because that section counts request-level failures only (bbg-data, optional).

41. **USD notional for crosses (pnl-engine, 2026-09-14).** `engine/pnl/aggregate.py::aggregate_by_pair` takes the USD notional magnitude from the trade's USD leg. A cross (EURSEK) has no USD leg, so `usd_leg_abs` is NaN and `groupby.sum()` currently turns it into `usd_notional = 0`, reporting the pair as flat (reviewer warning W-1). CLAUDE.md derives cross notional as EUR amount x EURUSD spot of `as_of_date` (item 14). No cross is in the 08/18 book. Default: surface NaN (`min_count=1`) now; derive via `SPOT` of the base ccy's USD pair when the xlsx ingest brings a cross in.
42. **No BNP-derived spot for AUD, EUR, GBP, XAU in the ladder `usd` column (cash-ladder, 2026-09-14).** `ladder_table(conn, as_of, source='BNP_BVAL')` leaves `usd` NaN for those four currencies because BNP `Fx = 1.0` on XXXUSD rows (item 32). The official path (`source=None`) is empty until Bloomberg marks land, so today the Cash ladder tab shows USD equivalents for 14 of 18 currencies under BNP_BVAL and none under Official. Not a code defect; resolves itself when bbg-data delivers `SPOT` marks. Related follow-up (reviewer W-5): `ladder_table` hard-codes positions source `'BNP'`; a CALC path needs a second parameter rather than overloading `source`.
43. **Realised P&L of trades settling between t-1bd and t (reviewer W-6, 2026-09-14).** `ltd_per_trade` keeps only legs with `settle_date > as_of`, so a trade that settles on t drops out of LTD(t) entirely and its P&L vanishes from daily / MTD / YTD differences. CLAUDE.md's period definitions do not address realised P&L; item 9 proposes freezing P&L at settlement and carrying it as realised. Needs a decision before the daily series is trusted across a settlement date (first one in the reference book: 2026-09-08).
44. **Holiday calendar for period_pnl (pnl-engine, 2026-09-14).** `period_pnl` uses a Mon-Fri weekday calendar for t-1bd, t-5bd, previous month end and previous year end; CLAUDE.md says "t-n bd uses the trading calendar". Which calendar: NY, London, or the union (FX settles on both)? Default: weekday only until decided; the reference dates are returned in the result so a wrong one is visible.
45. **Reject double-counting in `data/load.py` (data-ingest, reviewer W-7, 2026-09-14).** A FORWARD row that fails the description regex is rejected once by `bnp.parse` and once by `extract_bnp_marks`, so the summary line reports `rejects=2` for one bad row (`tests/test_ingest.py` locks this in). Options: de-duplicate `reject_details` by `row_no` before counting, or report `trade_rejects=` and `mark_rejects=` separately. Default: keep the raw count until decided; exit code is unaffected.
46. **Which tab hosts the P&L view (ui-shell, 2026-09-14).** `ui/tabs/pnl.py` (per-pair notional / LTD / n_trades, Net-Gross-gold-futures totals, daily / 5d / MTD / YTD with reference dates) is wired into **Overall book**, matching CLAUDE.md "P&L rollups by strategy / account, LTD series". The FX tab ("per-pair USD notional") stays empty for now and would show per-pair marks per leg rather than P&L rollups. Confirm the split, or move the per-pair table to FX and keep only totals and periods on Overall book.
47. **USD-equivalent column of the transposed Cash ladder (ui-shell, reviewer W-5, 2026-09-14).** `ui/tabs/cash_ladder.py::transpose_ladder` blanks a date's `usd_equivalent` when any currency with a non-zero amount on that date lacks a spot (zeros never block); it never sums a partial date. Because `ladder_table` exposes only `total` and `usd`, the tab back-derives spot as `usd / total` (`_implied_spot`), which is NaN for any currency whose ladder total nets to zero (a swap, or offsetting forwards at different dates) and then blanks every date that currency appears on, even though the engine knows the spot. Hidden today because only BNP_BVAL SPOT exists (item 42). Proposed: cash-ladder adds a per-currency `spot` column (or a `spot_table`) to `engine/ladder/views.py` and the tab drops `_implied_spot`.
48. **Partial commit on rejects in `data/load.py` (data-ingest, reviewer W-1, 2026-09-14).** The CLI calls `bnp.load(strict=False)`, so when a file has a regex-failing row the good rows are committed and the run still exits 1; a rerun then reports those rows as `skipped`, masking the earlier failure. Options: `strict = not --allow-rejects` (nothing written on rejects unless the flag is given), or document the partial-commit semantics in the docstring and `--help`. Related (reviewer W-3): the `--marks` path is untested, writes `<input>.tmp_filtered.csv` next to the user's input file, and reports reject row numbers of the filtered temp file rather than the original.
49. **Ownership of `data/load.py` (housekeeper, reviewer W-4, 2026-09-14).** CLAUDE.md's table owns `data/ingest/` and `data/bloomberg/` only; the user assigned `data/load.py` to data-ingest for this task, and the module imports `data/bloomberg/bnp_marks` and `marks_csv` internals. Options: move it to `data/ingest/cli.py` (`py -3 -m data.ingest.cli`), or add `data/load.py -> data-ingest` to the ownership table in CLAUDE.md. Also `DEFAULT_DB = Path("data/raw/risk.db")` is cwd-relative while `ui/app.py` resolves from the repo root; the CLI should resolve from `__file__` so the UI finds the DB when run from elsewhere.
51. **Duplicate blpapi session (bbg-data, 2026-09-15, follow-up not blocking).** `data/bloomberg/rates_marketdata.py` opens its own `blpapi.Session`, separate from `pull_marks.py`'s. Not merged deliberately — session-management consolidation is a bigger refactor than the IRS integration needed and risked destabilizing the working FX pull. Follow-up: merge into one shared session once both pull paths are proven stable.
52. **`realised_pnl.mark_type` gap (rates-pricer, 2026-09-15).** Wherever the app enumerates `mark_type` for realised P&L history, it currently lists `SPOT | FUTURE_PX` only, not `PAR_RATE`/`PV_USD` — so IRS marks are not yet wired into any realised-P&L history view. Out of scope for Phase 1 (realised P&L history isn't part of what's being wired up now); noted so it isn't lost when that view is built. **RESOLVED 2026-09-17:** IRS and FX_OPTION rows are now in `value_book` and `realise_settled`; a `CASHFLOW_USD` official mark (net settled coupons) keeps IRS LTD continuous across payments and at maturity; `pull_once` runs a rates step (OIS quotes + fixings + `price_all_and_store`) and an options step before `realise_settled`.
53. **Non-USD IRS notional not spot-converted (rates-pricer, 2026-09-15).** `engine/rates`'s `price_and_store` does not yet convert `PV_USD`/`DV01_USD` via spot for an IRS traded in a non-USD notional currency (marks are stored as-is, effectively assuming USD notional). Doesn't bite today — all three reference-file IRS trades are USD notional — but must be fixed before a non-USD swap is priced. Flagged as a Phase 2 item alongside term-rate/basis/XCCY. **RESOLVED 2026-09-17:** `price_and_store` converts PV / DV01 / cashflows at official SPOT via `engine.pnl.valuation.usd_per_quote` and raises when no SPOT is on file.

55. **RESOLVED 2026-09-16 (by exclusion, not dedupe) — trade-identity clash between `bnp.py`/`irs.py` and `blotter.py`.** The underlying scheme mismatch still exists exactly as described below (not solved), but its double-counting *consequence* is now closed off structurally: `data/ingest/schema.py`'s `trades_official` view (`SELECT * FROM trades WHERE source != 'BNP'`) is read by every engine query that computes live exposure/P&L/delta — `engine/ladder/ladder.py` (`_LEG_SQL`, `_DELTA_SQL`), `engine/ladder/exposure_adapter.py`, `engine/ladder/futures_delta.py`, `engine/ladder/valuation.py`, `engine/pnl/pnl.py::ltd_per_trade`, `engine/pnl/ledger.py`, `engine/pnl/valuation.py`, `ui/tabs/rates.py::irs_rows` — instead of raw `trades`, so a BNP-sourced copy of a trade the blotter also carries is never summed. `data/ingest/swaps.py::package_swaps`'s FORWARD-pairing groups now include `source` in the grouping key (reads raw `trades`, not `trades_official`, since BNP-internal swap pairing is still legitimate) so a genuine BNP outright can never be fabricated into a fake FX_SWAP with an unrelated blotter outright that happens to match by coincidence. The sole intentional exception is `engine/pnl/reconcile.py`, which explicitly needs both sources to compare them (see item 60) and reads raw `trades` by design. Original finding, still true as background: `data/ingest/bnp.py` (FORWARD) and `data/ingest/irs.py` (BNP path, IRS) key `trades.trade_id` off the id embedded in the BNP `Symbol` column (`Instrument Id`, not `Trade Id`); `data/ingest/blotter.py` keys the same trades off the blotter's own `Trade Id` column. These remain two different numbering schemes — a true per-trade dedupe (recognising "this BNP row and this blotter row are the same real trade" and merging them into one row) is not implemented and may never be needed now that BNP trades are structurally excluded from every live calculation; revisit only if a future feature needs to treat a BNP-sourced trade as authoritative for exposure again. Tests: `tests/test_trades_official.py` (the view + the actual double-count scenario end to end), `tests/test_ingest.py::test_swap_never_pairs_trades_from_different_sources`. Full suite 526 passed, 1 skipped after the fix.
56. **RESOLVED 2026-09-16 (by removal) — `trades.source = 'XLSX'` no longer means two different files.** `data/ingest/xlsx_futures.py` (the workbook futures-fill loader, `trade_id='XL-<row>'`) was deleted 2026-09-16 as dead code (its only caller, `ui/tabs/reconciliation.py`, was already gone, and the blotter now carries futures fills directly — see item 57 and item 11). `source='XLSX'` on a `trades` row now means only "from `data/ingest/blotter.py`". `blotter.py`'s own docstring still flags `'XLSX'` as an imperfect fit in the free-text `source` enum for a blotter row (a dedicated `'BLOTTER'` value would be clearer) — that naming point stands on its own regardless of the ambiguity being resolved.
57. **RESOLVED 2026-09-17 (settled, final state — reversed from the 2026-09-16 dual-format entry below) — the app's upload control accepts the blotter ONLY; BNP upload is removed entirely.** Full history: wired dual-format (09-16) → narrowed to blotter-only same day ("i really dont want the bnp file at all anywhere") → restored to dual-format same day once the user clarified the EOD-lag rationale ("both valid... check they reconcile") → **reversed again 2026-09-17** once the user confirmed directly what the cash ladder is actually for: "delta exposure and cashflow timing... scenario analysis," never a literal cash balance. Since that's the only thing BNP was still being kept for (item 59's original resolution), and the blotter already fully covers delta/cashflow timing/P&L/all-trades on its own, BNP has no remaining job in the live app at all. Final design: `data/ingest/upload.py` no longer contains `import_report`/`read_report`/`BNP_REQUIRED`/`detect_format`/`sniff_format`/`suggested_date` — not unreachable, deleted; the module is blotter-only end to end (`_parse_frame`/`preview_frame`/`validate_blotter_shape`/`import_blotter`). `data/ingest/bnp.py` itself is untouched, still used as a library by `data/load.py`'s CLI and `2_launcher.py`'s `sample` dev command (switched to `import_blotter` + a new checked-in `data/sample/blotter_sample.csv`, since `data/raw/` is gitignored and the launcher needs a stable sample on a fresh clone) and `data/bloomberg/bnp_marks.py`. Item 55's trade-identity clash is moot again in normal use (only one source is ever live) — see CLAUDE.md's "BNP file → tables" section, now marked historical. Items 59 and 60 below are reopened as a result. Same instruction ("make it as flexible as possible") also drove real robustness fixes to the import path itself, not just the removal — see the new item 62. Suite 524 passed, 1 pre-existing unrelated failure (`test_bloomberg_diagnostic.py`, environment-dependent, not caused by this change), 0 skipped, after the final change.

59. **REOPENED 2026-09-17, then closed as "not needed" — cash-ladder cash-balance column has no data source, and that's now fine.** Briefly broken (blotter-only pivot, 09-16), restored (dual-format, same day), broken again by item 57's final 09-17 reversal (BNP upload removed for good, so nothing writes `positions` rows any more). This time it's not being fixed, because the user confirmed directly that he doesn't need this column at all — the cash ladder he wants is delta exposure and cashflow timing (already fully covered by `trade_legs` alone, no BNP needed), not a bank-style balance. The column/query itself (`engine/ladder/ladder.py::cash_ladder()`'s `positions`-sourced `CASH` rows) is left in place, not deleted — it degrades gracefully to empty rather than erroring, and deleting it is a separate decision from "stop needing it" that hasn't been asked for. If a real balance source is ever wanted again (a genuine broker/custodian statement, or a capital-draw ledger for the pool/allocation model discussed with the user), this is where it would plug back in.

60. **REOPENED 2026-09-17 — EOD blotter-vs-BNP reconciliation check is now permanently inert, by design, not a bug.** Built 2026-09-16 (`engine/pnl/reconcile.py::reconcile_blotter_vs_bnp`, `ui/tabs/cash_ladder.py::reconciliation_panel`) to compare the two sources at end of day. Since item 57's 2026-09-17 reversal, BNP is never uploaded any more, so `bnp_snapshot_date()` always returns `None` and the panel always shows its one-line "no BNP snapshot loaded yet" state — there is nothing left for it to compare against, by design, not a regression. Left in place rather than deleted (same reasoning as item 59: "stop needing a second source" and "remove the code that compared two sources" are different decisions, and only the first has actually been asked for). If BNP or any other second source is ever reintroduced, this check starts working again with no code change needed.

**Same-day follow-up (user finding, 2026-09-16) — lag between BNP's once-daily EOD-Hong-Kong snapshot and the real-time blotter, fixed.** Comparing both sides through the Ladder tab's live `as_of_date` (which can be "today") would flag every blotter trade booked since BNP's last snapshot as a false mismatch, since BNP simply hasn't caught up yet — not a real discrepancy. Fixed: `engine/pnl/reconcile.py::bnp_snapshot_date(conn, as_of_date)` resolves the latest date BNP has actually reported through (preferring `MAX(positions.as_of_date)` where `source='BNP'`, falling back to `MAX(trades.trade_date)` where `source='BNP'` for a BNP file containing only IRS rows, which write no `positions` row), and `reconcile_blotter_vs_bnp` nets both sides through that resolved date, never the caller's raw `as_of_date`. Returns an empty frame (not a wall of flagged mismatches) if BNP has never reported at all. The Ladder tab panel names the actual date compared when it differs from the selected date, so "everything agrees" isn't misread as "today's trades were checked." Tests: `tests/test_pnl_reconcile.py` (`bnp_snapshot_date` resolution/fallback/future-date-exclusion, and the actual lag-exclusion scenario).

**RESOLVED 2026-09-16 (same day, on user request) — both reviewer findings above fixed.** (a) `data/ingest/upload.py`: the duplicated backup-and-upsert-and-swap-package block in `import_report`/`import_blotter` is now one shared `_stage_and_publish(db_path, load_fn)` helper; each caller supplies only its loader-specific `load_fn` (BNP's conflict-detection-and-marks step, or blotter's duplicate-key-on-reupload handling) and gets back whatever `load_fn` returns. No behaviour change, `tests/test_upload.py` unchanged and passing. (b) `ui/tabs/cash_ladder.py::_render`: `exposure_records_from_db`'s `unresolved` list (previously discarded as `_exposure_unresolved`) is now merged into the grid's own `unresolved` (deduped by `trade_id`) before being passed to `exposure_section`. Investigating this surfaced that the "Unresolved trades N" caption itself (`ui/tabs/exposure.py::metadata_line`) is dead code — never called from `exposure_section`, per that earlier 2026-09-15 decision's own docstring ("metadata line... retired, not moved") — so this fix corrects the data reaching `exposure_section`'s `unresolved` parameter (in case anything downstream ever uses it) rather than restoring a visible warning that doesn't currently exist anywhere in the UI; `metadata_line` itself was left alone (reviving a retired UI element is a different decision than fixing a data-completeness bug, not made here). Tests: `tests/test_ui_ladder.py::test_render_no_longer_discards_exposure_only_unresolved_trades`. Full suite 527 passed, 1 skipped after both fixes.

50. **RESOLVED 2026-09-14 — `data/load.py` conflict detection completed.** Headline `conflicts=` is now `sum(bnp_res.conflicts.values())` (trades + legs + positions) plus marks conflicts, so Local-Cost-only, cash-balance and futures amendments feed the exit code; both float comparators are 1e-9 absolute with no relative term. Tests added for a Local-Cost-only amendment (conflicts=2: leg amount + positions cost_local) and a CURRENCY balance amendment (conflicts=1); existing amended-fill and amended-Price tests now expect 3 and 2. Real file loaded twice still gives `skipped=262 conflicts=0`. Suite 160 passed. Original finding: `data/load.py` conflict detection incomplete after pass-2 review (data-ingest, 2026-09-14). The C-1 fix compares existing rows on re-load and reports `conflicts=`, but (a) `data/load.py:190-195, 220` sums only `conflicts["trades"]` and marks into the headline count and exit code, so an amended `Local Cost` (which changes only `trade_legs.amount` and `positions.cost_local`), an amended cash balance or futures settlement price (CURRENCY / FUTURES rows create no `trades` row) prints `conflicts=0`, rc 0 — verified by the reviewer on the real file (`DOL.C-USAA` +1000 → bnp.load returns `positions: 1`, CLI reports 0); (b) `_CONFLICT_TOL_REL = 1e-6` in `data/ingest/bnp.py:590-605` (and `_values_close` in `load.py:41`) is a 568-unit band on a 568m JPY leg, so a 1-unit Local Cost amendment passes as a skip. Exact CSV→SQLite REAL round-trip means zero/1e-9 absolute tolerance still gives `skipped=262 conflicts=0` on an identical re-load. Remedy (two one-line changes plus tests): `n_conflicts = sum(bnp_res.conflicts.values()) + marks conflicts`; drop the relative term in both comparators; add tests for a Local-Cost-only amendment (mind the `|Q×rate − Local Cost| ≤ 1` recon: choose the sign that keeps deviation < 1) and a CURRENCY-balance amendment. Also untested: `_load_marks_csv_idempotent` conflicts on the `--marks` path; its `try/except (TypeError, ValueError)` at `load.py:139-142` is dead code. Not applied because the review budget (two passes) was exhausted; needs a user decision to run another round.
58. **New (housekeeper, 2026-09-16) — `engine/pnl/pnl.py`'s literal workbook arithmetic is currently unreachable from the UI.** CLAUDE.md's "User-authorised direction, 2026-09-15" section keeps this module and retains it explicitly for "the Reconciliation tab only," but the Reconciliation tab (`ui/tabs/reconciliation.py`) was deleted 2026-09-16 at the user's explicit direction. `engine/pnl/pnl.py` was deliberately NOT deleted alongside it this session because `ltd_per_trade`/`workbook_valuation_date` are genuinely imported by `engine/ladder/valuation.py` and `data/bloomberg/marks_csv.py` (the live ladder and marks pipeline) — so the module does real, still-used work outside Reconciliation, contrary to CLAUDE.md's framing of it as Reconciliation-only. What's actually orphaned is narrower: any code path that exists solely to expose the literal workbook-parity arithmetic for on-screen reconciliation display no longer has a caller. Not investigated further here (out of scope for this task — any change to `engine/pnl/pnl.py` needs the full agent pipeline per CLAUDE.md's "Working mode" section, invoked only by the user). Needs a decision: keep the module's reconciliation-display arithmetic dormant, wire it into a different tab, or have the user confirm what (if anything) should still show it.

61. **New (housekeeper, 2026-09-17) — options_calc merge, Phase 0 landed.** User's standalone QuantLib option/rates-derivative pricing library (`C:\Users\jeanw\Jean Bourgeois`, ~4,300 lines: FX/equity/commodity vanilla+exotic option pricers, swaptions/caps/SABR/Bermudan, VolSurface, Position/Portfolio aggregation) is being merged in full, phased across 9 phases (0–8), not trimmed to a subset — full plan in agent memory `options-calc-merge-2026-09-17` and `engine/options/__init__.py`'s scope ledger. Phase 0 (this entry): library vendored as-is into `engine/options/vendor/options_calc/` (plus `vendor/MODELS.md`, the upstream spec), two new agents registered (`options-pricer` owning `engine/options/`, `rates-exotics` for the later swaption/cap/SABR/Bermudan phase, directory TBD), `scipy` added to `2_launcher.py`'s `PACKAGES`/`IMPORT_CHECKS` (an undeclared transitive dependency of the vendored library's implied-vol solvers) and installed. Also resolved as part of this: the standing conflict between CLAUDE.md's "Six tabs as views" table and `ui/app.py`'s docstring over whether Options is a top-level tab — settled as neither; options will show as a grouped, collapsible summary inside the Blotter tab (Portfolio Totals → asset class → structure/package → legs), per a Bloomberg MARS-style reference layout the user supplied. Both CLAUDE.md and `ui/app.py` updated to state this consistently. **Update, same day — Phases 1–4 landed** (suite 556 passed + the known unrelated Bloomberg-diagnostic failure). Phase 1: `instrument_options` table + blotter strike/call-put parsing (chosen over widening `instruments` to spare ~70 fixtures). Phase 2: `engine/options/{pricer,inputs,store}.py` price FX VANILLA/DIGITAL from `trades_official` and write PREMIUM/DELTA/GAMMA/THETA/VEGA/RHO marks, `source='QL_OPTIONS_PRICER'`, now the official source for all six (MANUAL demoted to reconciliation-only). Phase 3: item 25 closed. Phase 4: AMERICAN/ASIAN/BARRIER_KI/KO/ONE_TOUCH/NO_TOUCH payoffs + `structures.py::combine_package`. **Open follow-ups from these phases:** (a) data-ingest — `_parse_option` never sets `barrier_level`/`avg_start_date`/`payoff` (always VANILLA), so exotic payoffs are exercised by synthetic rows only; and no rule yet assigns a shared `package_id` to legs of a straddle/risk-reversal (FX-swap rule is the precedent — needs a decision on the matching keys: same pair, trade date, expiry, counterparty, opposite CALL/PUT?). (b) Inputs are placeholders by design until Phase 5/7: rates from `options_calc.fx.rate_curves` (illustrative, not live), vol from the manual `option_vols` table (flat per pair/expiry; `set_manual_vol`). (c) Asian: `avg_start_date` is read but the vendored engine has no custom averaging-start parameter (documented limitation). (d) Premium unit: `PREMIUM` marks are base-ccy fraction of base notional to match `trades.price` (blotter `Price` × notional = `NetInvoice` in base ccy), i.e. vendored quote-ccy price ÷ spot. (e) Option premium cash flows are not on the cash ladder (options legs are `settles_cash = 0`); whether the premium itself should be a ladder row is undecided. **Phase 5 landed same day** (suite 586 passed): `data/bloomberg/vol_marketdata.py` pulls ATM/RR25/BF25/RR10/BF10 per tenor into a `vol_quotes` staging table (file-backed fixture for offline tests; `--probe` CLI for the terminal), and `engine/options/inputs.py::resolve_vol` prices off the vendored `FXDeltaVolSurface` (SMILE) → ATM interpolation → manual `option_vols` → skip. **UNVERIFIED until run on the Bloomberg machine**: the 9-item ticker/field probe list in the module docstring (`EURUSDV1M BGN Curncy`, `EURUSD25R1M`, `EURUSD25B1M`, 10d variants, `PX_LAST`, `ON` tenor validity, live-vs-historical split, raw points scale) — run `py -m data.bloomberg.vol_marketdata --db data/raw/risk.db --probe` there and tick them off, same discipline as items 27/28. Rates fed to the option pricer remain the illustrative `options_calc.fx.rate_curves` placeholders (Phase 7). **Phase 6 landed same day** (suite 628 passed; reviewer pass clean): `engine/rates_vol/` (new agent rates-exotics) prices European swaptions (Black-76), SABR-vol swaptions, caps/floors and Bermudan swaptions (one-factor Hull-White tree) from the vendored `options_calc.rates`, with the forward swap rate taken off the bootstrapped OIS curve and flat discounting to expiry (the vendored engine is flat-curve by design — fine for risk, not a mid quote). Inputs are manual tables (`rate_vols` lognormal only — NORMAL quotes are rejected, not converted; `rate_model_params` for SABR and Hull-White a/sigma), option terms in `instrument_rate_options`. Known limitations: Bermudan exercise dates are regenerated from an inferred frequency because the vendored pricer takes a frequency, not a date list — do not feed Bermudan marks into P&L/hedging without a reviewer pass; and **no trade source carries swaptions or caps** (blotter Fin Types don't include them), so the trade shape (`product` SWAPTION/CAP_FLOOR, `asset_class` IRS_OPTION) is exercised by tests only until an ingest path is decided. **Phases 7 and 8 + Phase 6 limits closed, same day** (suite 703 passed). Phase 7: `engine/options` prices equity (`EQ_OPTION`) and commodity (`CMDTY_OPTION`) options, aggregates a book via the vendored `Portfolio` with Greeks converted to USD-equivalents (`portfolio.py`), persists strike x tenor `VolSurface` points, uses the pair's joint holiday calendar for year fractions and writes `DELTA_PA` for premium-adjusted G10 pairs; illustrative rates are gone -- FX options take continuously-compounded zero rates off the bootstrapped OIS curve (exact for Garman-Kohlhagen), and currencies without an OIS convention (SEK/NOK/ZAR/TWD ...) need a `manual_rates` entry (`engine/options/rates.py::set_manual_rate`) or skip "no curve/rate <CCY>" -- follow-up for rates-pricer: add SWESTR/NOWA to `engine/rates/conventions.py`. Phase 6 limits: the upstream library (the user's `Jean Bourgeois` folder, now 238 tests) gained `discount_curve`/`forecast_curve` handles, a restoring `evaluation_date` scope and explicit `exercise_dates`, and was re-vendored; `engine/rates_vol` prices off the real OIS curve handle (no flat approximation), Bermudans use the trade's staged exercise dates, `calibration.py` fits SABR (scipy) and Hull-White (QuantLib helpers) from the `rate_vols` grid (MANUAL params override CALIBRATED), `py -m engine.rates_vol.book` books a swaption/cap manually, and `data/bloomberg/rates_vol_marketdata.py` pulls swaption/cap vols into `rate_vol_quotes` with its own 9-item UNVERIFIED probe list (`--probe`). Phase 8: `ui/tabs/options.py` renders the Blotter's Options sub-tab as Portfolio Totals -> asset class -> package -> leg with expand/collapse (verified against a running Dash server), hooked into `ui/tabs/blotter.py` like the rates scope. **The options_calc merge plan is complete.** Remaining, all outside code: run both `--probe` commands on the Bloomberg terminal (FX vol and rates vol tickers); decide a trade source for rate/equity/commodity options (manual booking only today); decide the package_id grouping rule for multi-leg FX option structures.

62. **New (2026-09-17) — blotter import made deliberately tolerant, per direct user instruction ("no import errors — nothing where the file gets rejected over something stupid — be as flexible as possible").** Concrete changes, all in `data/ingest/upload.py` and `data/ingest/blotter.py`: (a) `encoding='utf-8-sig'` on both CSV read paths — an Excel "CSV UTF-8" export's leading byte-order-mark used to corrupt the first column name silently, making a perfectly good file look completely unrecognized; (b) required-column matching (`validate_blotter_shape`) is case-/whitespace-insensitive; (c) `_canonicalize_columns` re-cases any incoming column that matches a reference blotter header name case-insensitively, so `data/ingest/blotter.py`'s own case-sensitive `row.get(...)` lookups keep working regardless of the source file's actual header casing, without needing to touch every lookup in that module; (d) `Status`/`Fund`/`Fin Type` row-level comparisons in `blotter.py` are now `.casefold()`/`.upper()`-normalized too, for the same reason at the row level; (e) a single-sheet Excel workbook needs no sheet argument any more (only a genuinely ambiguous multi-sheet workbook asks); (f) **`import_blotter` now calls `blotter.load(..., strict=False)` instead of `strict=True`** — previously one malformed row anywhere in an 800+ row file blocked the entire import; now every row that parses cleanly loads regardless, and every row that doesn't is still named in the returned message (row number, symbol, reason) rather than silently dropped or blocking everything else. Tests: `tests/test_upload.py` (canonicalization, case/whitespace tolerance, BOM tolerance against the real reference file, and a synthetic one-bad-row-doesn't-block-the-file case). This work happened alongside a peer Claude session's unrelated Phase 1 options_calc changes to the same file (`blotter.py`); coordinated via cross-session messaging to avoid overlapping edits, no conflict in the end (peer's changes were to option-column parsing, this session's were to encoding/case-handling in unrelated code paths).

63. **New (rates-pricer, 2026-09-17) — Rates recon column needs a typed-in SWPM PV.** blpapi has no per-trade SWPM valuation; `ui/tabs/rates.py` now accepts a MANUAL `PV_USD` (Market data tab manual entry) as the recon figure alongside `BBG_BDH`, so the fund's SWPM PV must be typed in by hand.

54. **Closed FORWARD lines, missing fills and NDF fixings (data-ingest / pnl-engine, 2026-09-15).** The 2026-09-15 BNP file (`HA_PNL_20260915[22].csv`) carries 49 FORWARD rows with `Quantity = 0`: BRL / TWD / KRW / IDR NDFs fixed 2026-09-14 for value 2026-09-16 and two settled USDHKD 2026-09-08 lines, each with zero MV but realised DTD / MTD P&L still on the line. The ingest rule now in CLAUDE.md ("closed line": positions row with the P&L, no trade, open-position identities not checked, no BNP_BVAL mark) was decided so the upload loads at all; two follow-ups need a call. (a) Six of those lines were traded after 2026-08-17 (BRL 197633180 and 198774932, HKD 198514883, IDR 198775404, KRW 198778053, TWD 198725343) and their fills are in no uploaded file, so `trades` has no notional for them and the Reconciliation tab will show BNP realised P&L against nothing on our side. Default: upload the intervening daily files, which carry the fills while the lines were live. (b) The engine values a fixed NDF at the `FWD_OUTRIGHT` for its value date until `settle_date`; economically it is fixed from the fixing date. Options: a `FIXING` mark that freezes the trade from fixing date, or accept the two-day drift. Default: current behaviour (outright until value date). Also new in this file: 15 `OPTION` rows (skipped as unsupported) — input for the Options tab — and `Price` printed to 5 dp, which is why the MV Local tolerance now scales with quantity. The same file also prints `Quantity` / `Local Cost` of unchanged trades with float noise (`3500000` on 08-18 became `3499999.999979`; `1157208.997` became `1157208.99749`), so the re-load conflict comparator of question 50 now uses a 0.01 absolute tolerance on amount columns (quantity, leg amount, cost, MV, P&L) and keeps 1e-9 on rates and marks; a half-unit amendment is still a conflict.
