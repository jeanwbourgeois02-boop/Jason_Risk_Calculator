data/raw/
__pycache__/
.pytest_cache/
*.db
*.bloomberg_status.json
reports/
.venv/
.claude/settings.local.json
# Trade files never go to git (user, 2026-09-22: "i dont want to commit any trade files"):
# a blotter dropped under data/ or at the root stays untracked. Subfolders are not
# matched, so data/sample/ (synthetic) and data/bbg_snapshot/ (marks) still are.
data/*.xlsx
data/*.xlsm
data/*.xls
data/*.csv
/*.xlsx
/*.xlsm
/*.xls
/*.csv
.idea/
